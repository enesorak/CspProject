<UserControl x:Class="CspProject.Views.TemplatesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CspProject.Views"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel Margin="30">
        <TextBlock DockPanel.Dock="Top" Text="Templates" FontSize="28" FontWeight="Light" Margin="0,0,0,20"/>
        <TextBlock DockPanel.Dock="Top" Text="Create a new document from an available template." FontSize="14" Foreground="Gray" Margin="0,0,0,20"/>
        <dxg:GridControl x:Name="TemplatesGrid" AutoGenerateColumns="None" MouseDoubleClick="TemplatesGrid_MouseDoubleClick">
            <dxg:GridControl.Columns>
                <dxg:GridColumn FieldName="Name" Header="Template Name"/>
                <dxg:GridColumn FieldName="Description" Header="Description"/>
            </dxg:GridControl.Columns>
            <dxg:GridControl.View>
                <dxg:TableView ShowGroupPanel="False" NavigationStyle="Row"/>
            </dxg:GridControl.View>
        </dxg:GridControl>
    </DockPanel>
</UserControl>
